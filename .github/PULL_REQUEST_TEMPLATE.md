## What does this change?

<!-- A sentence or two. Link any related issue with "Closes #123". -->

## Why?

<!-- What problem does it solve? -->

## How was it tested?

<!-- CI only covers pure logic, since GitHub runners have no display.
     Anything display-related needs manual verification. -->

- [ ] `swift build` succeeds
- [ ] `swift test` passes
- [ ] `swift run understudy-probe` passes all checks. **macOS version:**
- [ ] Not applicable

## Checklist

- [ ] Comments explain *why* where the code isn't self-evident
- [ ] `CHANGELOG.md` updated under `Unreleased`, if user-visible
- [ ] Any new private-API use stays inside `Sources/CVirtualDisplay/`
